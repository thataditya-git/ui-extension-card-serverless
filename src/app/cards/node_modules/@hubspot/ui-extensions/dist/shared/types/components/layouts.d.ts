import type { ReactNode } from 'react';
import type { AllDistances, BaseComponentProps } from '../shared.ts';
/**
 * The props type for [Stack](https://developers.hubspot.com/docs/apps/developer-platform/add-features/ui-extensibility/ui-components/standard-components/flex).
 *
 * @category Component Props
 */
export interface StackProps extends BaseComponentProps {
    /**
     * Sets the spacing between components.
     */
    distance?: AllDistances;
    /**
     * Sets the content that will render inside the component.
     */
    children?: React.ReactNode;
    /**
     * Arranges components `horizontally` or `vertically` by setting the main axis.
     */
    direction?: 'row' | 'column';
    /**
     * Distributes components along the main axis using the available free space.
     */
    justify?: 'center' | 'end' | 'start';
    /**
     * Distributes components along the cross-axis using the available free space.
     */
    align?: 'start' | 'center' | 'baseline' | 'end' | 'stretch';
    /**
     * Sets the width of the stack.
     */
    width?: 'auto' | '100%';
}
export type FlexJustify = 'center' | 'end' | 'start' | 'around' | 'between';
export type FlexAlign = 'start' | 'center' | 'baseline' | 'end' | 'stretch';
/**
 * The props type for [Flex](https://developers.hubspot.com/docs/apps/developer-platform/add-features/ui-extensibility/ui-components/standard-components/flex).
 *
 * @category Component Props
 */
export interface FlexProps extends BaseComponentProps {
    /**
     * Sets the spacing between components.
     *
     * @defaultValue `"flush"`
     */
    gap?: AllDistances;
    /**
     * Sets the content that will render inside the component. This prop is passed implicitly by providing sub-components.
     *
     */
    children?: ReactNode;
    /**
     * Arranges the components horizontally or vertically by setting the main axis.
     *
     * @defaultValue `"row"`
     */
    direction?: 'row' | 'column';
    /**
     * Distributes components along the main axis using the available free space.
     *
     * @defaultValue `"start"`
     */
    justify?: FlexJustify;
    /**
     * Distributes components along the cross-axis using the available free space.
     *
     * @defaultValue `"stretch"`
     */
    align?: FlexAlign;
    /**
     * Distributes a child component along the cross-axis using the available free space. Use this prop for nested `Flex` and `Box` components to align them differently from other child components in the `Flex` group.
     */
    alignSelf?: FlexAlign;
    /**
     * Whether components will wrap rather than trying to fit on one line.
     *
     * @defaultValue `"nowrap"`
     */
    wrap?: boolean | 'wrap' | 'nowrap';
}
/**
 * The props type for [Box](https://developers.hubspot.com/docs/apps/developer-platform/add-features/ui-extensibility/ui-components/standard-components/box).
 *
 * @category Component Props
 */
export interface BoxProps extends BaseComponentProps {
    /**
     * Sets the content that will render inside the component. This prop is passed implicitly by providing sub-components.
     *
     */
    children: ReactNode;
    /**
     * Distributes a child component along the cross-axis using the available free space. Use this prop for nested `Box` components to align them differently from other child components in the `Flex` group.
     *
     * @defaultValue `"auto"`
     */
    alignSelf?: 'start' | 'center' | 'baseline' | 'end' | 'stretch' | 'auto';
    /**
     * Distributes components based on the available empty space around them. Options are as follows:
     * - `initial`: The item is sized according to its width and height properties. It shrinks to its minimum size to fit the container, but does not grow to absorb any extra free space in the flex container.
     * - `auto`: The item is sized according to its width and height properties, but grows to absorb any extra free space in the flex container, and shrinks to its minimum size to fit the container.
     * - `none`: The item is sized according to its width and height properties. It is fully inflexible: it neither shrinks nor grows in relation to the flex container.
     * - Number: tells a component to fill all available space, shared evenly amongst other components with the same parent. The larger the flex given, the higher the ratio of space a component will take compared to its siblings.
     *
     * @defaultValue `"auto"`
     */
    flex?: 'initial' | 'auto' | 'none' | number;
}
/**
 * The props type for [Inline](https://developers.hubspot.com/docs/apps/developer-platform/add-features/ui-extensibility/ui-components/standard-components/inline).
 *
 * @category Component Props
 */
export interface InlineProps extends BaseComponentProps {
    /**
     * Sets the content that will render inside the component. This prop is passed implicitly by providing sub-components.
     */
    children?: ReactNode;
    /**
     * Distributes components along the main axis using the available free space.
     *
     * @defaultValue `"start"`
     */
    justify?: FlexJustify;
    /**
     * Distributes components along the cross-axis using the available free space.
     *
     * @defaultValue `"stretch"`
     */
    align?: FlexAlign;
    /**
     * The amount of spacing between components.
     *
     * @defaultValue `"flush"`
     */
    gap?: AllDistances;
}
export interface PaddingProps {
    /**
     * Sets padding on all four sides.
     */
    padding?: Exclude<AllDistances, 'flush'>;
    /**
     * Sets padding on the left and right sides. When `padding` is also set, `padding` takes precedence and this prop is ignored.
     */
    paddingInline?: Exclude<AllDistances, 'flush'>;
    /**
     * Sets padding on the top and bottom sides. When `padding` is also set, `padding` takes precedence and this prop is ignored.
     */
    paddingBlock?: Exclude<AllDistances, 'flush'>;
}
export interface MarginProps {
    /**
     * Sets margin on the top side.
     */
    marginTop?: AllDistances;
    /**
     * Sets margin on the bottom side.
     */
    marginBottom?: AllDistances;
    /**
     * Sets margin on the left side.
     */
    marginLeft?: AllDistances;
    /**
     * Sets margin on the right side.
     */
    marginRight?: AllDistances;
}
export interface SpacingProps extends PaddingProps, MarginProps {
}
/**
 * The props type for [AutoGrid](https://developers.hubspot.com/docs/apps/developer-platform/add-features/ui-extensibility/ui-components/standard-components/autogrid).
 *
 * @category Component Props
 */
export interface AutoGridProps extends BaseComponentProps {
    /**
     * Sets width of each column in pixels. When `flexible` is `true`, acts as minimum width before expanding.
     */
    columnWidth: number;
    /**
     * Sets spacing between grid items.
     *
     * @defaultValue `"flush"`
     */
    gap?: AllDistances;
    /**
     * Sets the content that will render inside the component. This prop is passed implicitly by providing sub-components.
     */
    children?: ReactNode;
    /**
     * When `false`, columns have exact `columnWidth`. When `true`, columns expand equally to fill available space with `columnWidth` as the minimum.
     *
     * @defaultValue `false`
     */
    flexible?: boolean;
}
