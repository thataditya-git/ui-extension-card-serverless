import { getWorkerGlobals } from "../../internal/global-utils.js";
import { VALID_HTTP_METHODS, } from "../../shared/types/api-client.js";
import { InvalidApiBodyError, InvalidApiContentTypeError, InvalidApiHeadersError, InvalidApiMethodError, InvalidApiPathError, } from "./errors.js";
export { VALID_HTTP_METHODS } from "../../shared/types/api-client.js";
const isPlainObject = (value) => value !== null && typeof value === 'object' && !Array.isArray(value);
const validate = (request) => {
    if (typeof request.path !== 'string' ||
        request.path.length === 0 ||
        !request.path.startsWith('/')) {
        throw new InvalidApiPathError(request.path);
    }
    if (!VALID_HTTP_METHODS.has(request.method)) {
        throw new InvalidApiMethodError(String(request.method));
    }
    if (request.headers !== undefined) {
        if (!isPlainObject(request.headers)) {
            throw new InvalidApiHeadersError(request.headers);
        }
        for (const [, value] of Object.entries(request.headers)) {
            if (typeof value !== 'string') {
                throw new InvalidApiHeadersError(request.headers);
            }
        }
    }
    if (request.contentType !== undefined &&
        typeof request.contentType !== 'string') {
        throw new InvalidApiContentTypeError(request.contentType);
    }
    if (request.body !== undefined && !isPlainObject(request.body)) {
        throw new InvalidApiBodyError(request.body);
    }
};
export const fetchHubSpotApi = (request) => {
    return new Promise((resolve, reject) => {
        try {
            validate(request);
        }
        catch (error) {
            return reject(error);
        }
        getWorkerGlobals()
            .hsWorkerAPI.fetchHubSpotApi(request)
            .then(resolve)
            .catch(reject);
    });
};
